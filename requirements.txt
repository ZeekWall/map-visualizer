# Basemap is rendered once by Cartopy, then the animation is pure OpenCV/PIL,
# so python_tsp, geopandas, networkx, tsplib95 and tabulate are no longer needed.
Cartopy==0.25.0
matplotlib==3.11.1
numpy==2.5.1
opencv-python-headless==4.13.0.92
pillow==12.3.0
pyproj==3.7.2
pyshp==3.1.6
requests==2.34.2
shapely==2.1.2

# tui.py -- optional, only needed to run the Textual workbench
textual==8.2.8
